# Tasks: Recipe Sections

## Tasks

- [ ] Task 1: Backend — add `IngredientSection` / `InstructionSection` types, extend `Recipe` model, add EF migration, update DTOs and controller
- [ ] Task 2: Backend — extend AI prompt and parser to extract and return sections
- [ ] Task 3: Frontend — update TypeScript types (`mock-data.ts`, `recipe.service.ts`) and recipe detail view to render sections
- [ ] Task 4: Frontend — update `RecipeForm` to support editing sections (add section, rename heading, add/remove items per section)
- [ ] Task 5: Frontend — wire upload/save-extracted flow to pass sections through
