# Tasks: Image Cropping Before Upload

## Tasks

- [ ] Task 1: Build `CropModal` component (`frontend/src/components/CropModal.tsx`) — canvas-based crop UI with drag-to-move, corner-handle resize, Crop/Skip actions, keyboard support (Enter/Escape), default centered-square selection
- [ ] Task 2: Integrate `CropModal` into `MainPhotoUpload` — intercept file selection to show crop modal, add "Crop" button to re-crop pending file
- [ ] Task 3: Integrate `CropModal` into `SortableInstruction` in `RecipeForm.tsx` — intercept photo selection to show crop modal before upload
- [ ] Task 4: Run inner loop — lint, typecheck, build; fix all errors
