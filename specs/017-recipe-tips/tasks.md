# Tasks: Recipe Tips

## Tasks

- [x] Task 1: Add `Tips` field to `Recipe` model and configure EF Core JSON mapping — generate and apply EF Core migration
- [x] Task 2: Update DTOs (`RecipeDto`, `RecipeFormRequest`) and `RecipesController` to include `tips`; update AI extraction prompt/schema
- [x] Task 3: Add `tips` to frontend TypeScript types (`Recipe` in mock-data, `RecipeFormData` in recipe.service) and tips editor UI in `RecipeForm`
- [x] Task 4: Render tips callout section on the recipe detail page (`client.tsx`)
