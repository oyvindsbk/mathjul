# Tasks: Navn og kallenavn

## Tasks

- [ ] Task 1: Add `Name` and `Nickname` to `User` entity and create EF Core migration (backend)
- [ ] Task 2: Extend `ensure-user` response to include `name` and `nickname`; add `UserController` with `PUT /api/user/{id}` (backend)
- [ ] Task 3: Extend `AuthContext` with `userId`, `name`, `nickname` state populated from `ensure-user` (frontend)
- [ ] Task 4: Create `/profil` page with profile settings form (frontend)
- [ ] Task 5: Add "Profil" link to Sidebar user dropdown pointing to `/profil`; update middleware public routes if needed (frontend)
