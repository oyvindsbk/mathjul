# Tasks: Breadcrumb Navigation

## Tasks

- [ ] Task 1: Rewrite `BreadcrumbBar` component — route-to-label mapping, `sessionStorage` referrer for recipe pages, hidden on mobile, sticky below header
- [ ] Task 2: Add `NavigationTracker` logic to store `lastListPage` in `sessionStorage` on list-page visits
- [ ] Task 3: Integrate `BreadcrumbBar` into `layout.tsx` between `<Sidebar />` and `<main>`
- [ ] Task 4: Remove per-page `<Breadcrumb>` usages from individual pages (favoritter, grupper, last-opp-oppskrift, etc.)
- [ ] Task 5: Run lint + typecheck + build; fix any failures
